import { MagyarDatumPipe } from './magyar-datum.pipe';

describe('MagyarDatumPipe', () => {
  it('create an instance', () => {
    const pipe = new MagyarDatumPipe();
    expect(pipe).toBeTruthy();
  });
});
